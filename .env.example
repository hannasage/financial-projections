# Storage mode: omit (or set to anything other than "pocketbase") for local-only demo mode.
# Set to "pocketbase" to enable backend auth and cloud plan sync.
VITE_STORAGE_MODE=local

# Only required when VITE_STORAGE_MODE=pocketbase
VITE_PB_URL=http://127.0.0.1:8090
